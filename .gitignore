# Agent work area (transient files)
.agent-workarea/

# Kubeconfig (generated by K3s deployment)
kubeconfig

# Ansible
*.retry
.vault_pass

# User inventories (keep only sample)
# This excludes production.yaml, staging.yaml, etc. - only sample_inventory.yaml is tracked
inventories/*
!inventories/sample_inventory.yaml

# Secrets
*.pem
*.key
secrets/

# Helm
helm-*.tgz
charts/
*.lock
