---
# Vault Configuration
# Variables for the vault role

# Enable/disable service
vault_enabled: true

# Release configuration
vault_release_name: "vault"
vault_chart: "hashicorp/vault"
vault_chart_version: "0.27.0"

# Storage settings
vault_storage_size: "10Gi"
vault_storage_class: "local-path"

# UI settings
vault_ui_enabled: true

# Dev mode (NEVER use in production!)
vault_dev_mode: false
